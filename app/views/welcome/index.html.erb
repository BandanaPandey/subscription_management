<div class="card" style="text-align: center;">
  <div class="card-header">
    <h5 class="card-title">Welcome</h5>
    <span style="float: right;">
      <% if user_signed_in? %>
        <%= current_user.email %>
        |
        <%= link_to('Logout', destroy_user_session_path, method: :delete) %>        
      <% else %>
        <%= link_to('Login', new_user_session_path) %>
      <% end %>
    </span>
  </div>
  <div class="card-body">
    <p class="card-text">Welcome To Ruby Subscription!!</p>
    <p class="card-text">You are on <b>GO PREMIUM </b>Page</p>
    <p style="padding: 103px;">
      <a href="#" class="btn btn-primary">Free Link 1</a>
      <a href="#" class="btn btn-primary">Free Link 2</a>
      <a href="<%= new_subscriber_path %>" class="btn btn-primary">Go Premium</a>
    </p>
  </div>
</div>