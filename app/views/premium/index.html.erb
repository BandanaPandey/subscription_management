<div class="card" style="text-align: center;">
  <div class="card-header">
    <h5 class="card-title">Welcome</h5>
    <span style="float: right;">
      <% if user_signed_in? %>
        <%= current_user.email %>
        |
        <%= link_to('Logout', destroy_user_session_path, method: :delete) %>        
      <% else %>
        <%= link_to('Login', new_user_session_path) %>
      <% end %>
    </span>
  </div>
  <div class="card-body">
    <p class="card-text">Welcome To Ruby Subscription.</p>
    <p class="card-text">You are on <b>MANAGE SUBSCRIPTION </b>Page, please click on <b>Manage Subscription</b> to see your subscription details</p></p>
    <p style="padding: 103px;">
      <a href="#" class="btn btn-primary">Free Link 1</a>
      <a href="#" class="btn btn-primary">Free Link 2</a>
      <a href="#" class="btn btn-primary">Premium Link 1</a>
      <a href="#" class="btn btn-primary">Premium Link 1</a>
      <a href="<%= subscriber_path(current_user.id) %>" class="btn btn-primary">Manage Subscription</a>
    </p>
  </div>
</div>